
export const APP_NAMESPACE = 'ConnectPhysio:v1';

export const LS_KEYS = {
  AUTH_SESSION: `${APP_NAMESPACE}:authSession`,
  // All other keys have been removed as the app now uses a real-time database.
  // Theme and sidebar state are managed by their respective components.
};
